cmake_minimum_required(VERSION 3.10)
project(EGT_Revolution_OS)

set(CMAKE_CXX_STANDARD 20)

# Core Lazarus Kernel Dependencies
add_executable(egt_lazarus_v56_2 kernel/egt_kernel_v56_2.cpp)

# Target-specific hardware optimizations
if(MSVC)
    target_compile_options(egt_lazarus_v56_2 PRIVATE /O2 /Oi)
else()
    target_compile_options(egt_lazarus_v56_2 PRIVATE -O3 -ffast-math -march=native)
endif()

message(STATUS "EGT v56.2 Build Configured for 12.09776 fT Sync")
